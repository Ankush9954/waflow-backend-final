events { }
http {
  server {
    listen 80;
    location /api/ {
      proxy_pass http://company-service:5001/api/;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
    }
  }
}
